// Externals
import styled from 'styled-components';
import { Box } from 'grid-styled';

// Variables
import { spaces } from '../../styles/variables';

export default styled(Box)`
  margin-left: auto;
  margin-right: auto;
  max-width: 1300px;
  padding-left: ${spaces[2]}px;
  padding-right: ${spaces[2]}px;
`;
