// Externals
import styled from 'styled-components';

// Variables
import { colors } from '../../styles/variables';

export const StyledFooter = styled.footer`
  background-color: ${colors.white};
  box-shadow: 1px 10px grey;
  position: fixed;
  width: 100%;
`;
